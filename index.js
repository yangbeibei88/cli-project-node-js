#!/usr/bin/env node
// above line is an entry into the app
// if import something which doesn't have export, then no need `from`
import "./src/command.js";
// import * as utils from "./utils";
// import fs from "node:fs";
// import http from "node:http";
// import path from "node:path";

// import { count } from "./utils/utils";

// const { count } = require("./utils");

// ES module to import fs

// Common Module to require fs
// const fs = require("fs");

// console.log("Hello World!");

// const note = process.argv[2];
// const newNote = {
//   content: note,
//   id: Date.now(),
// };
// console.log(newNote);
// console.log(count);
// console.log(global);
// console.log(process.argv);
// console.log(process.env.NODE_ENV);
